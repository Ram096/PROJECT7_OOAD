public class Enums {
    public enum StaffType {Driver, Manager, Cook, Cashier}
    public enum DayOfWeek {Mon,Tue,Wed,Thur,Fri,Sat,Sun}
    public enum BuyerType {pickUp, delivery}
    public enum Topping {pepperoni, mushroom, onion, sausage, bacon, olives, peppers}
    public enum Sauce {marinara, alfredo, bbq, ranch}
    public enum Crust {thin, regular, deep_dish}
    public enum Size {small, medium, large, xLarge}
    public enum cookCond {under_cooked, just_Right, over_cooked}
    public enum Student {student, not_student}
}
